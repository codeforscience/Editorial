{{!< default}}

{{#post}}
{{!-- Everything inside the #post tags pulls data from the page --}}


<section>

    <header class="main content">
        <h1>{{title}}</h1>
    </header>

    {{#if feature_image}}
    <div class="image main"><img style="max-width:100%;width:auto;margin:0 auto;" src="{{img_url feature_image}}" alt="{{title}}" /></div>
    {{/if}}

    <div class="content">
        {{content}}
    </div>

</section>


{{/post}}
